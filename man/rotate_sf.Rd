% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-spatial.R
\name{rotate_sf}
\alias{rotate_sf}
\title{Rotate spatial features for plotting BC coastline}
\usage{
rotate_sf(sf_obj, angle = -40, lonc = -129)
}
\arguments{
\item{sf_obj}{An sf object to rotate}

\item{angle}{Rotation angle in degrees (default: -40). Negative values rotate clockwise.}

\item{lonc}{Central meridian longitude (default: -129, roughly central for
the BC coast). If you're layering multiple rotated sf objects on the same
plot, keep \code{lonc} fixed across all of them -- each object's own bbox
midpoint will generally differ, and rotating layers to different central
meridians will misalign them. Pass \code{lonc = NULL} to compute that
per-object midpoint instead (reported via message), e.g. to choose a better
midpoint \code{lonc} value for your data.}
}
\value{
sf object in rotated oblique Mercator projection
}
\description{
Rotates sf objects using oblique Mercator projection to align BC's
northwest-southeast coastline with plot axes.
}
\examples{
\dontrun{
coast <- pacea::bc_coast
coast_rotated <- rotate_sf(coast, angle = -40)

ggplot(coast_rotated) + geom_sf()
}
}
