# # TODO add SQL here:
# pbs_areas <- areas
# pbs_species <- species
# names(pbs_areas) <- tolower(names(pbs_areas))
# names(pbs_species) <- tolower(names(pbs_species))
# devtools::use_data(pbs_areas, overwrite = TRUE, internal = FALSE)
# pbs_species$species_common_name <- gsub("SPINY DOGFISH", "NORTH PACIFIC SPINY DOGFISH", pbs_species$species_common_name)
# devtools::use_data(pbs_species, overwrite = TRUE, internal = FALSE)
