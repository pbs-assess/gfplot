# TODO add SQL here:
# names(pbs_areas) <- tolower(names(pbs_areas))
# names(pbs_species) <- tolower(names(pbs_species))
# devtools::use_data(pbs_areas, overwrite = T, internal = TRUE)
# devtools::use_data(pbs_species, overwrite = T, internal = TRUE)
